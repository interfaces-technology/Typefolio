# Typefolio web (`@typefolio/web`)

API-only product client: library, specimen, devices, plan, and account.

```bash
npm run dev:api
npm run dev:web
```

Local URL: http://127.0.0.1:43126

`/api/*` rewrites to `NEXT_PUBLIC_API_URL` so the session cookie stays on this origin.
